
const add = (a, b) => {
    return a + b
};

const dupliactes = (num) => {
    const dupliactes = num.map((item) => item * 2);
    return dupliactes
}

const name = "Vijay"


module.exports = { add, name, dupliactes }
